
function ProductList({name, city}){
    // const {name, city} = props;
    
    return(
         <div>
            {
            <h3>This is {name},lives in {city} </h3> 
            }
            
        </div>
    );
}

export default ProductList;
